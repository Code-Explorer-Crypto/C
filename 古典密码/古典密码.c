#include <stdio.h>
#define MAX 100
int main() {
	int a = 0;
	int arr[MAX] = { 0 };
	//ด๓ะด
	if (arr[0] >= 65 &&arr[0] <= 90) {
		for (int i = 0;i < sizeof(arr);i++) {

		}
	}
	//ะกะด
	else if (arr[0] >= 97 && arr[0] <= 122) {

	}
  }	